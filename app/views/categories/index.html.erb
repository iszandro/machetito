<h1 class="title">Categories</h1>

<ul class="categories">
  <% @categories.each do |category| %>
    <li class="category">
      <%= link_to category.display_name, category_path(category) %>
    </li>
  <% end %>
</ul>
