<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item" href="https://bulma.io">
      <img src="https://icon-library.net/images/japan-icon/japan-icon-2.jpg" width="30" height="50">
    </a>

    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to 'Home', root_path, class: "navbar-item" %>
      <% categories.each do |category| %>
        <%= link_to category.display_name, category_path(category), class: "navbar-item" %>
      <% end %>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <% unless user_signed_in? %>
          <div class="buttons">
            <a class="button singup">
              <strong>Sign up</strong>
            </a>
            <a class="button is-light">
              Log in
            </a>
          </div>
        <% end %>

        <% if user_signed_in? %>
          <div class="navbar-item has-dropdown is-hoverable">
            <a class="navbar-link">
              <%= current_user.email %>
            </a>
            <div class="navbar-dropdown">
              <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "navbar-item" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
